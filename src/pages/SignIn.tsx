import { Box } from "@mui/material";

const SignIn = () => {
    return (
        <Box sx={{ display: 'flex', justifyContent: 'center', mt: 10 }}>
            Happy happy happy
        </Box>
    );
};

export default SignIn;